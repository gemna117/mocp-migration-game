using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LoadOnClick : MonoBehaviour
{
    public GameObject loadingCanvas;
    
    public void loadcanvas(int level)
    {
        loadingCanvas.SetActive(true);
        Canvas.load
    }
}
